name: auth_manager
description: "Example app for Auth Ninja SDK"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  auth_ninja_sdk:
    path: ../
  flutter_riverpod: ^3.0.3
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.1.1
  google_fonts: ^6.3.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
